This folder include the files and documentation for installing the proxyApp with `Guix` package manager.  

```
.
├── guix
│   ├── manifest
│   ├── configs
│   └── docs
```

The three folders store   
- `manifest`: some examples of manifest files  
- `configs`:  the local channel with the package derivations  
- `docs` the documentation.    